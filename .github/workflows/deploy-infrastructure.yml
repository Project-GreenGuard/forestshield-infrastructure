name: Deploy Infrastructure

on:
  push:
    branches: [main, develop]
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to deploy'
        required: true
        type: choice
        options:
          - staging
          - production

env:
  AWS_REGION: us-east-1

jobs:
  deploy:
    name: Deploy Infrastructure
    runs-on: ubuntu-latest
    environment: ${{ github.ref == 'refs/heads/main' && 'production' || 'staging' }}
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.5.7"

      - name: Terraform Init
        run: terraform init

      - name: Determine Environment
        id: env
        run: |
          if [ "${{ github.ref }}" == "refs/heads/main" ] || [ "${{ github.event.inputs.environment }}" == "production" ]; then
            echo "environment=production" >> $GITHUB_OUTPUT
          else
            echo "environment=staging" >> $GITHUB_OUTPUT
          fi

      - name: Terraform Plan
        run: |
          terraform plan \
            -var="environment=${{ steps.env.outputs.environment }}" \
            -out=tfplan \
            -no-color

      - name: Terraform Apply
        run: terraform apply -auto-approve tfplan

      - name: Get Terraform Outputs
        id: outputs
        run: |
          echo "api_endpoint=$(terraform output -raw api_endpoint)" >> $GITHUB_OUTPUT
          echo "s3_bucket=$(terraform output -raw s3_bucket_name)" >> $GITHUB_OUTPUT
          echo "frontend_url=$(terraform output -raw frontend_url)" >> $GITHUB_OUTPUT
          echo "cloudfront_id=$(terraform output -raw cloudfront_distribution_id)" >> $GITHUB_OUTPUT
          echo "iot_endpoint=$(terraform output -raw iot_core_endpoint)" >> $GITHUB_OUTPUT

      - name: Output Deployment Info
        run: |
          echo "✅ Infrastructure deployed to ${{ steps.env.outputs.environment }}!"
          echo "API Endpoint: ${{ steps.outputs.outputs.api_endpoint }}"
          echo "Frontend URL: ${{ steps.outputs.outputs.frontend_url }}"
          echo "S3 Bucket: ${{ steps.outputs.outputs.s3_bucket }}"
          echo "CloudFront ID: ${{ steps.outputs.outputs.cloudfront_id }}"
          echo ""
          echo "📝 Add these values as GitHub Secrets in backend and frontend repos:"
          echo "  ${{{ steps.env.outputs.environment | upcase }}}_API_URL=${{ steps.outputs.outputs.api_endpoint }}"
          echo "  ${{{ steps.env.outputs.environment | upcase }}}_S3_BUCKET=${{ steps.outputs.outputs.s3_bucket }}"
          echo "  ${{{ steps.env.outputs.environment | upcase }}}_CLOUDFRONT_ID=${{ steps.outputs.outputs.cloudfront_id }}"

